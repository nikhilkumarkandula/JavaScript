# JavaScript Projects Collection

This repository contains various JavaScript projects I’ve worked on. Each folder includes its own project with a README and instructions for setting up and running the application.

## Projects

### 1. **Project 1: Notes app**
   - **Description**: A notes app to store the important to do things on web app for future use.
   - **Features**:
     - Add new notes
     - Persistant data
     - No data loss on refresh
   - **Tech Stack**:
     - JavaScript (ES6+)
     - Node.js (if applicable)
     - HTML/CSS

   #### Installation
   To run this project locally, follow these steps:
   1. Clone the repository:
      ```bash
      git clone https://github.com/your-username/repository-name.git
      ```
   2. Go to directory:
      ```bash
      cd Notes App
      ```
   3. Install dependencies:
      ```bash
      npm install
      ```
   4. Run the application:
      ```bash
      npm start
      ```
---

### 2. **Project 2: Quiz App**
   - **Description**: A samle Quiz demo how js can be used to develop quiz applications.
   - **Features**:
     - DOM manipulation
     - File naviagtion
     - Event handlers
   - **Tech Stack**:
     - JavaScript
     - HTML/CSS
   
   #### Installation
   1. Clone the repository:
      ```bash
      git clone https://github.com/your-username/repository-name.git
      ```
   2. Go to directory:
      ```bash
      cd Quiz App
      ```
   3. Install dependencies:
      ```bash
      npm install
      ```
   4. Run the app:
      ```bash
      npm run dev
      ```

---

